#!/usr/bin/env bash
source /dev/stdin < <(cli loader)
source /dev/stdin < <(cli initialize)
source /dev/stdin < <(cli export)

help() {
    cat << EOF
Command
    integrate area .group

Summary
    Commands for finding areas of shapes.

Global Arguments
    --help -h        [Flag] : Show this message and exit.
    --self-test      [Flag] : Runs a self test.
EOF
}

main() {
    math::square() {
        math::multiply $1 $1
    }

    cli::export::import integrate .group
    cli::export math
}

self_test() {
    source /dev/stdin < <(cli::import cli_assert)
}

cli::load "$@"